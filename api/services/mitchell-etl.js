/**
 * Mitchell 1 ETL — placeholder until full sync is wired.
 * Export getCustomerHistory for /api/customers/:id/history
 */
async function getCustomerHistory(workspaceId, customerId) {
  return null;
}

module.exports = { getCustomerHistory };
